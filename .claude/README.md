# .claude/ — bon-vivant-api

Config de Claude Code para el backend.

- **`../CLAUDE.md`** — fuente de verdad del repo: stack, estructura, endpoints, estado actual.
- **`standards.md`** — reglas no negociables de seguridad y calidad Python. Léelo antes de codear.
- **`settings.json`** — permisos (uvicorn/pytest/ruff/black/pip/supabase) + `systemPrompt`
  que recuerda leer `standards.md`.

Contexto compartido entre repos: `../../CLAUDE.md` (paraguas).
